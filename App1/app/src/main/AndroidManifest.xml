<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/icone"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/icone"
        android:supportsRtl="true"
        android:theme="@style/Theme.InsatlKotlinV1"
        android:usesCleartextTraffic="true">
        
        <!-- Activité principale -->
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        
        <!-- Liste des appartements -->
        <activity
            android:name=".pages.ListAppartementActivity"
            android:exported="false" />
        
        <!-- Ajout/Modification d'appartement -->
        <activity
            android:name=".pages.AddEditAppartementActivity"
            android:exported="false" />
        
        <!-- Statistiques -->
        <activity
            android:name=".pages.StatsActivity"
            android:exported="false" />
            
    </application>

</manifest>